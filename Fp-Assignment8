 # Write a fibonacci function using recursion (no for loops). Keep on testing this function with higher and higher inputs. Do you get stack overflow error? Why?
def fibonacci_funct(i):
	if i == 0:
		return [1,1]

	lst=fibonacci_funct(i-1)
	print lst[0]
	return [lst[1],lst[0]+lst[1]]
	
i=10
fibonacci_funct(i)
